@page
@model Patricia_Adelina_web.Pages.Vizionari.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Vizionare[0].Vizionator)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Vizionare[0].Film)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Vizionare[0].DataVizionare)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Vizionare) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Vizionator.ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Film.Titlu)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DataVizionare)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
